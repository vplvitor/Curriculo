package control;

public class PessoaControl {
	// Regra de Negocio de Pessoa
	public boolean addPessoa(String nome, String email) {
		if(nome != null && nome.matches(email)) {
			
		}
	}
}
